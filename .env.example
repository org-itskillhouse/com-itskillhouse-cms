# A generated secret for Payload
PAYLOAD_SECRET=

# Auth.js secret (openssl rand -hex 32)
AUTH_SECRET=

# Microsoft Entra ID app registration
ENTRA_CLIENT_ID=
ENTRA_CLIENT_SECRET=
ENTRA_TENANT_ID=

# Public auth URL used by Auth.js when app is mounted under /cms
AUTH_URL=https://itskillhouse.com/cms/api/auth

# Auth.js base path used by client/session endpoints
AUTH_BASE_PATH=/cms/api/auth
